Overview
========
This directory contains some sample input files for the PaperDelivery package, this is also used to
prepare the database that is used by the test suite, and reduces the need to create the SQL that
is used to create the database manually.


